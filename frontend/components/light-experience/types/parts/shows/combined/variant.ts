import { VariantUI } from '@mora-light/core/types/transmit';

export type TrimmedVariantViewConstraint = {
    name: 'VariantView'; // variant view

    // ? creating
    ui?: VariantUI; // how to show ?
};

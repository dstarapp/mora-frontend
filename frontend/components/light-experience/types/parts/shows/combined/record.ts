import { RecordUI } from '@mora-light/core/types/transmit';

export type TrimmedRecordViewConstraint = {
    name: 'RecordView'; // record view

    // ? creating
    ui?: RecordUI; // how to show ?
};
